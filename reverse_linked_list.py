from typing import List


class ListNode:
    def __init__(self, val = 0, next = None):
        self.val = val
        self.next = next

'''
reversing in recursion 
1 2 3 4 5 6

recursive call until the next or current node is null - if so, return node itself
  
  


reverse(node): 
  if 
  reverse(node)
base case (recursive call stop case)


'''
linkedList = new ListNode(1)


def reverseList(node: ListNode):
  return 0
